#!/bin/sh

POOLNAME=tank
RELEASE=10.1-RELEASE

iocage activate ${POOLNAME}

if [ ! -e /iocage/releases/${RELEASE}/root ]; then
    iocage fetch release=${RELEASE}
fi

sysrc -f /etc/rc.conf.d/iocage iocage_enable="YES"
